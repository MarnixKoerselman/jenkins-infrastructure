pipeline {
    agent {
        node 'ssh-agent'
    }
    stages {
        stage('test') {
            steps {
                sh 'ls -al'
                sh 'git --version'
            }
        }
    }
}
